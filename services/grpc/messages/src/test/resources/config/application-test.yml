spring:
  cloud:
    consul:
      enabled: false
  grpc:
    server:
      inprocess:
        name: test
    client:
      channel:
        inProcess:
          target: "in-process:test"

management:
  metrics:
    use-global-registry: false
  otlp:
    metrics:
      export:
        enabled: false