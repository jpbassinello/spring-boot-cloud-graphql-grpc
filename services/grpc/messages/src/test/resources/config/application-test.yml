spring:
  cloud:
    consul:
      enabled: false
  grpc:
    server:
      inprocess:
        name: test
    client:
      channels:
        inProcess:
          address: "in-process:test"

otel:
  sdk:
    disabled: true

management:
  metrics:
    use-global-registry: false
