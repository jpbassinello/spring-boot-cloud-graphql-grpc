spring:
  cloud:
    consul:
      enabled: false
  grpc:
    server:
      inprocess:
        name: test
    client:
      channels:
        inProcess:
          address: "in-process:test"

management:
  metrics:
    use-global-registry: false
  otlp:
    metrics:
      export:
        enabled: false