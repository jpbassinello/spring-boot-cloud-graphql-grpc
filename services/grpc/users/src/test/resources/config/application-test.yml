spring:
  cloud:
    consul:
      enabled: false

otel:
  sdk:
    disabled: true

management:
  metrics:
    use-global-registry: false

keycloak:
  active: false
  server-url: http://keycloak
  username: username
  password: password

grpc:
  server:
    port: -1
    in-process-name: test
  client:
    inProcess.address: in-process:test
