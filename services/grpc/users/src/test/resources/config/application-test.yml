spring:
  cloud:
    consul:
      enabled: false
  grpc:
    server:
      inprocess:
        name: test
    client:
      channels:
        inProcess:
          address: "in-process:test"

otel:
  sdk:
    disabled: true

management:
  metrics:
    use-global-registry: false

keycloak:
  active: false
  server-url: http://keycloak
  username: username
  password: password
