spring:
  cloud:
    consul:
      enabled: false

management:
  metrics:
    use-global-registry: false
  otlp:
    metrics:
      export:
        enabled: false

keycloak:
  active: false
  server-url: http://keycloak
  username: username
  password: password
